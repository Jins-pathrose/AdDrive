import 'package:flutter/material.dart';

class ImageUploads extends StatelessWidget {
  const ImageUploads({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Image Uploads'),
      ),
    );
  }
}